const data = require('../data/zoo_data');

const getEmployeesCoverage = (name) => {

};
  // const mudar = data.employeer.filter((chave) => chave === Object.keys )
  // const pessoa = data.employees.filter((pes) => pes.lastName || pes.firstName || pes.id);
  // const mudar
  // if (pessoa){
  //   return pessoa.find((obj) => obj.lastName && obj.firstName && obj.id && obj.responsibleFor && obj.location);
  // }
  // return achar;

// getEmployeesCoverage('Sharonda');
module.exports = getEmployeesCoverage;
