const data = require('../data/zoo_data');

const getEmployeesCoverage = (name) => {
// const pessoa = data.employees.find((pes) => pes.id === pes.lastName);
// const achar = pessoa.employees.map((pes) => pes.id )
// console.log(pessoa);
//     return pessoa;
// };

// getEmployeesCoverage("Sharonda Spry");
};
module.exports = getEmployeesCoverage;
